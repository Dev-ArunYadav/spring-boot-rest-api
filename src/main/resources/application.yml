spring:
  application:
    name: spring-boot-rest-api
  datasource:
    url: jdbc:mysql://localhost:3306/spring_tds
    username: root
    password: Taruna@taxlogic
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect